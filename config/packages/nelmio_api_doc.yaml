nelmio_api_doc:
    documentation:
        info:
            title: North Health
            description: Api to access North Health company database
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
